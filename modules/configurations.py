# general variables
#------------------------------------------------------------------------------
guest_fraction = 1.0
host_fraction = 1.0
experiments_fraction = 1.0


# Define how to save the extracted data (output mode)
# 'CSV' = save data in .csv file
# 'SQL' = save data as SQL dataset
#------------------------------------------------------------------------------
output_type = 'CSV'
SQL_config = {'username' : 'username', 
              'password' : 'password'}
